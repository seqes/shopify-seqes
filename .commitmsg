fix: resolve 53 integration issues in ported sections

Color schemes (21 sections):
- Replace non-functional color-{{ scheme }} classes with direct color pickers
- Add bg_color + text_color settings to section and block schemas
- Generate scoped CSS rules (section-color-{{ section.id }}) for backgrounds
- Handle block-level colors via inline styles with Liquid variables
- Promote global settings references to section-level settings

Custom elements JS (6 sections):
- Add <comparison-slider> before/after drag interaction
- Add <bundle-deals> checkbox toggling + variant selection + price recalc
- Add <promo-popup> show/dismiss popup with delay + localStorage
- Add <internal-video> lazy-load video sources on IntersectionObserver
- Add <section-group> move section content into side-by-side containers

Missing snippets:
- Add icon-discount.liquid SVG for card-product and price snippets

Source-theme cleanup:
- Remove content-for-grouping class from image-banner, email-signup-banner
- Remove animate-item classes from 5 sections
- Clean up separate-atc-btn.liquid animation class reference
- Fix malformed Liquid tag in product-card-size-options.liquid
